import { z } from 'astro/zod';

/**
 * Frontmatter block for Talks & Media (board 8c).
 * OWNER: the Talks & Media work unit. Empty until that page is built.
 */
export const talksMediaBlock = z.object({}).strict();
