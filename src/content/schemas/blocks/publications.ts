import { z } from 'astro/zod';

/**
 * Frontmatter block for Publications (board 7a).
 * OWNER: the Publications work unit. Empty until that page is built.
 */
export const publicationsBlock = z.object({}).strict();
