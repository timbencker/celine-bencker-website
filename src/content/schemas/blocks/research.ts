import { z } from 'astro/zod';

/**
 * Frontmatter block for Research (board 10b).
 * OWNER: the Research work unit. Empty until that page is built.
 */
export const researchBlock = z.object({}).strict();
